# Core dependencies from CodeCrash
anthropic==0.69.0
colorama==0.4.6
datasets==4.1.1
numpy==1.26.4
openai==2.2.0
python-dotenv==1.1.1
sglang==0.5.2
tenacity==9.1.2
tqdm==4.67.1
pybase64>=1.3.0

# GPT trace generation dependencies
markdown>=3.4.0
pandas>=2.0.0

# Training and inference dependencies
torch>=2.0.0
transformers>=4.30.0

# Note: LLaMA-Factory should be installed separately
# git clone https://github.com/hiyouga/LLaMA-Factory.git
# cd LLaMA-Factory && pip install -e .
